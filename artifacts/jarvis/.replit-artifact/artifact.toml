kind = "web"
previewPath = "/"
title = "Jarvis"
description = "Sistema personal de gestión financiera para registrar ingresos, gastos, kilometraje y consultar el resumen mensual."
version = "1.0.0"
id = "artifacts/jarvis"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21662

[services.development]
run = "pnpm --filter @workspace/jarvis run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/jarvis", "run", "build" ]
serve = "static"
publicDir = "artifacts/jarvis/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21662"
BASE_PATH = "/"
