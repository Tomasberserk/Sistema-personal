# Frontend de Jarvis

La aplicación React ejecutable vive en `artifacts/jarvis`, donde Replit registra
su artifact web y su vista previa. Este directorio documenta la separación
solicitada entre `backend/` (FastAPI + SQLite) y `frontend/` (React).